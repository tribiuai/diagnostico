{
  "name": "diagnostico-ia-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AI Business Diagnostic App",
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
